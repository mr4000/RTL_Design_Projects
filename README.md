# RTL Design Projects

This repository contains multiple RTL design mini-projects.  
Each project has:

- RTL code (`rtl/`)
- Testbench (`tb/`)
- Simulation results (`results/`)
- Documentation (`README.md` inside each project)

Projects are stored under the `projects/` directory.

